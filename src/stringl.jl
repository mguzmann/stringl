module Stringl

export str_to_upper, str_to_lower, str_to_title, str_to_fupperend, str_to_flower, str_replace, str_count, str_length, str_detect, str_c, str_replace, str_replace_all

include("string-functions.jl")
include("additional-functions.jl")

end
